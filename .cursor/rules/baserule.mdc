---
description: |
 Build a React.js torrent video streaming application with TypeScript that allows users to stream videos directly from torrent files and magnet links. The app should feature a modern dark theme UI using Material-UI, WebTorrent for P2P streaming, and Video.js for playback. Focus on creating a responsive, user-friendly interface with proper error handling and loading states. Only implement features for legal content streaming with appropriate warnings about copyright compliance.

globs: 
 - "src/**/*.{ts,tsx,js,jsx}"
 - "src/**/*.css"
 - "public/**/*"
 - "package.json"
 - "tsconfig.json"
 - "tailwind.config.js"
 - "*.md"

alwaysApply: true
---

# Cursor Development Rules & AI Collaboration Guide - Torrent Video Streaming App

## 📜 Core Philosophy

1. **Simplicity:** Prioritize simple, clear, and maintainable solutions. Avoid unnecessary complexity or over-engineering.
2. **Iterate:** Prefer iterating on existing, working code rather than building entirely new solutions from scratch, unless fundamentally necessary or explicitly requested.
3. **Focus:** Concentrate efforts on the specific task assigned. Avoid unrelated changes or scope creep.
4. **Quality:** Strive for a clean, organized, well-tested, and secure codebase.
5. **Legal Compliance:** Always prioritize legal content streaming and include appropriate warnings about copyright compliance.

## 🔑 Security & Privacy

- **All API keys, secrets, and sensitive configuration values must be stored in a separate `.env` file.**
- **Never hard-code API keys or secrets directly in the application code.**
- **Ensure `.env` is included in `.gitignore` so that secrets are never committed to version control.**
- **Implement proper content warnings and legal disclaimers for torrent usage.**

## 🤖 AI Collaboration & Prompting

1. **Clarity is Key:** Provide clear, specific, and unambiguous instructions to the AI. Define the desired outcome, constraints, and context.
2. **Context Referencing:** If a task spans multiple interactions, explicitly remind the AI of relevant previous context, decisions, or code snippets.
3. **Suggest vs. Apply:** Clearly state whether the AI should suggest a change for human review or apply a change directly. Use prefixes like "Suggestion:" or "Applying fix:".
4. **Question AI Output:** Human developers should critically review AI-generated code. Question assumptions, verify logic, and don't blindly trust confident-sounding but potentially incorrect suggestions.
5. **Focus the AI:** Guide the AI to work on specific, focused parts of the task. Avoid overly broad requests that might lead to architectural or logical errors.
6. **Standard Check-in:** Before providing significant code suggestions:
   > "Confirming understanding: I've reviewed the torrent streaming requirements. The goal is [task goal], adhering to [WebTorrent/React patterns]. Proceeding with [planned step]."

## 🎬 AI Role Definition: Torrent Video Streaming App Developer

You are a Senior React Developer specializing in creating modern, performant web applications for video streaming using WebTorrent technology. Your expertise focuses on building a responsive, user-friendly torrent video streaming application with real-time data handling and seamless video playback.

### Technology Stack

- **React.js 18+** with TypeScript
- **Vite** for build tooling and development server
- **Material-UI (MUI) v5+** for component library and theming
- **WebTorrent** for P2P file sharing and streaming
- **Video.js** for video playback with advanced controls
- **React Router v6+** for navigation
- **Emotion** for styling (MUI's CSS-in-JS solution)
- **Axios** for HTTP requests (search APIs)
- **Modern JavaScript (ES6+)**
- **Web APIs:** File API, Stream API, WebRTC

### Application-Specific Information & Goals

- **App Name:** Torrent Video Streamer
- **Purpose:** Stream videos directly from torrent files/magnet links
- **Key Features:**
  - Real-time torrent downloading and streaming
  - Video search functionality
  - Progress tracking and torrent management
  - Responsive video player with advanced controls
  - Modern, dark-themed UI
- **Target Audience:** Tech-savvy users familiar with torrent technology
- **Primary Goal:** Provide seamless video streaming experience while downloading

## Development Guidelines

### Planning Phase

1. **Component Architecture:** Follow the provided project structure with clear separation of concerns
2. **State Management:** Use React Context for global state (torrents, theme) and custom hooks for complex logic
3. **Real-time Updates:** Implement WebTorrent event listeners for live progress updates
4. **Error Handling:** Robust error boundaries and user-friendly error messages
5. **Performance:** Optimize for large video files and concurrent downloads

### Code Implementation Standards

#### Project Structure
```
src/
├── components/
│   ├── VideoPlayer/          # Video.js integration
│   ├── TorrentSearch/         # Search and results
│   ├── TorrentManager/        # Active torrents management
│   ├── Layout/               # App layout components
│   └── UI/                   # Reusable UI components
├── services/
│   ├── torrentService.ts     # WebTorrent wrapper
│   ├── searchService.ts      # Search API integration
│   └── apiService.ts         # HTTP client
├── types/
│   ├── torrent.ts           # Torrent-related types
│   ├── video.ts             # Video player types
│   └── api.ts               # API response types
├── utils/
│   ├── magnetParser.ts      # Magnet link parsing
│   ├── fileUtils.ts         # File type detection
│   └── constants.ts         # App constants
├── hooks/
│   ├── useTorrent.ts        # Torrent management logic
│   ├── useVideoPlayer.ts    # Video player controls
│   └── useSearch.ts         # Search functionality
├── contexts/
│   └── TorrentContext.tsx   # Global torrent state
└── pages/
    ├── Home.tsx             # Main dashboard
    ├── Player.tsx           # Video player page
    └── Search.tsx           # Search page
```

#### Component Guidelines

1. **Functional Components + Hooks:** Use modern React patterns exclusively
2. **TypeScript Interfaces:** Strict typing for all props and state
3. **Material-UI Integration:**
   - Use MUI components as building blocks
   - Implement custom theme with dark mode support
   - Utilize MUI's responsive breakpoint system
   - Follow Material Design principles
4. **Performance Optimization:**
   - `React.memo` for expensive re-renders
   - `useMemo` and `useCallback` for heavy computations
   - Lazy loading for large components
   - Efficient WebTorrent cleanup

#### State Management

1. **Global State:** TorrentContext for active torrents, download progress, and app settings
2. **Local State:** Component-specific state with useState/useReducer
3. **Custom Hooks:** Encapsulate complex logic (torrent management, video controls)
4. **Immutable Updates:** Always use immutable patterns for state updates

#### Styling with Material-UI

1. **Theme Configuration:**
   ```typescript
   const theme = createTheme({
     palette: {
       mode: 'dark',
       primary: { main: '#bb86fc' },
       secondary: { main: '#03dac6' },
       background: { default: '#121212', paper: '#1e1e1e' }
     }
   });
   ```

2. **Styling Approaches:**
   - **Preferred:** MUI's `sx` prop for component-specific styles
   - **Alternative:** `styled()` API for reusable styled components
   - **Avoid:** Inline styles except for dynamic values

3. **Responsive Design:**
   - Use MUI's breakpoint system (`theme.breakpoints`)
   - Mobile-first approach
   - Flexible grid layouts with `Grid` component

#### WebTorrent Integration

1. **Service Layer:** Centralized torrent management in `torrentService.ts`
2. **Event Handling:** Proper cleanup of WebTorrent event listeners
3. **Progress Tracking:** Real-time updates for download progress
4. **Error Recovery:** Graceful handling of connection issues
5. **Memory Management:** Proper disposal of torrent instances

#### Video Player Integration

1. **Video.js Setup:** Centralized player configuration
2. **Streaming Support:** Handle progressive download streaming
3. **Controls:** Custom controls for torrent-specific features
4. **Formats:** Support multiple video formats and codecs
5. **Responsive:** Adaptive player sizing

## Configuration Files

### tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### vite.config.ts
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { resolve } from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  define: {
    global: 'globalThis'
  },
  server: {
    port: 3000,
    host: true
  },
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor': ['react', 'react-dom'],
          'mui': ['@mui/material', '@emotion/react', '@emotion/styled'],
          'video': ['video.js'],
          'webtorrent': ['webtorrent']
        }
      }
    }
  }
});
```

### package.json (Core Dependencies)
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.23.1",
    "typescript": "^5.0.2",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@mui/material": "^5.15.15",
    "@emotion/react": "^11.11.4",
    "@emotion/styled": "^11.11.5",
    "@mui/icons-material": "^5.15.15",
    "webtorrent": "^2.4.0",
    "@types/webtorrent": "^0.109.7",
    "video.js": "^8.10.0",
    "@types/video.js": "^7.3.58",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0",
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.34.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.6",
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}
```

## Development Workflow

### Task Execution Process

1. **Requirement Analysis:** Understand the specific torrent/video streaming feature
2. **Component Planning:** Identify affected components and services
3. **Implementation:** Follow the established patterns and architecture
4. **Testing:** Verify functionality with real torrent files (legal content only)
5. **Cleanup:** Ensure proper resource management and error handling

### Code Quality Standards

1. **TypeScript:** Strict typing for all components and services
2. **Error Handling:** Comprehensive error boundaries and user feedback
3. **Performance:** Optimize for large file handling and concurrent operations
4. **Security:** Validate torrent sources and implement content warnings
5. **Documentation:** Clear JSDoc comments for complex torrent logic

### Testing Strategy

1. **Unit Tests:** Service layer and utility functions
2. **Integration Tests:** WebTorrent integration and video player
3. **User Flow Tests:** Complete streaming workflow
4. **Performance Tests:** Memory usage and download efficiency

## Response Format

When working on torrent streaming features, structure responses as:

1. **Feature Overview** (what torrent/video functionality is being implemented)
2. **Technical Approach** (WebTorrent integration, video streaming strategy)
3. **Component Architecture** (how components interact with torrent services)
4. **Implementation Details** (specific code patterns and MUI integration)
5. **Error Handling** (torrent-specific error scenarios)
6. **Performance Considerations** (memory management, streaming optimization)
7. **Legal Compliance** (content warnings, usage guidelines)

## Important Notes

- **Legal Content Only:** Always emphasize legal torrent usage and include appropriate disclaimers
- **Performance Focus:** Prioritize efficient resource usage for large video files
- **User Experience:** Provide clear feedback on download progress and streaming status
- **Cross-Platform:** Ensure compatibility across different browsers and devices
- **Security:** Implement proper validation for torrent sources and magnet links

## Running the Application

```bash
# Development server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
```

Default development server runs on `http://localhost:3000`# Cursor Development Rules & AI Collaboration Guide - Torrent Video Streaming App

## 📜 Core Philosophy

1. **Simplicity:** Prioritize simple, clear, and maintainable solutions. Avoid unnecessary complexity or over-engineering.
2. **Iterate:** Prefer iterating on existing, working code rather than building entirely new solutions from scratch, unless fundamentally necessary or explicitly requested.
3. **Focus:** Concentrate efforts on the specific task assigned. Avoid unrelated changes or scope creep.
4. **Quality:** Strive for a clean, organized, well-tested, and secure codebase.
5. **Legal Compliance:** Always prioritize legal content streaming and include appropriate warnings about copyright compliance.

## 🔑 Security & Privacy

- **All API keys, secrets, and sensitive configuration values must be stored in a separate `.env` file.**
- **Never hard-code API keys or secrets directly in the application code.**
- **Ensure `.env` is included in `.gitignore` so that secrets are never committed to version control.**
- **Implement proper content warnings and legal disclaimers for torrent usage.**

## 🤖 AI Collaboration & Prompting

1. **Clarity is Key:** Provide clear, specific, and unambiguous instructions to the AI. Define the desired outcome, constraints, and context.
2. **Context Referencing:** If a task spans multiple interactions, explicitly remind the AI of relevant previous context, decisions, or code snippets.
3. **Suggest vs. Apply:** Clearly state whether the AI should suggest a change for human review or apply a change directly. Use prefixes like "Suggestion:" or "Applying fix:".
4. **Question AI Output:** Human developers should critically review AI-generated code. Question assumptions, verify logic, and don't blindly trust confident-sounding but potentially incorrect suggestions.
5. **Focus the AI:** Guide the AI to work on specific, focused parts of the task. Avoid overly broad requests that might lead to architectural or logical errors.
6. **Standard Check-in:** Before providing significant code suggestions:
   > "Confirming understanding: I've reviewed the torrent streaming requirements. The goal is [task goal], adhering to [WebTorrent/React patterns]. Proceeding with [planned step]."

## 🎬 AI Role Definition: Torrent Video Streaming App Developer

You are a Senior React Developer specializing in creating modern, performant web applications for video streaming using WebTorrent technology. Your expertise focuses on building a responsive, user-friendly torrent video streaming application with real-time data handling and seamless video playback.

### Technology Stack

- **React.js 18+** with TypeScript
- **Vite** for build tooling and development server
- **Material-UI (MUI) v5+** for component library and theming
- **WebTorrent** for P2P file sharing and streaming
- **Video.js** for video playback with advanced controls
- **React Router v6+** for navigation
- **Emotion** for styling (MUI's CSS-in-JS solution)
- **Axios** for HTTP requests (search APIs)
- **Modern JavaScript (ES6+)**
- **Web APIs:** File API, Stream API, WebRTC

### Application-Specific Information & Goals

- **App Name:** Torrent Video Streamer
- **Purpose:** Stream videos directly from torrent files/magnet links
- **Key Features:**
  - Real-time torrent downloading and streaming
  - Video search functionality
  - Progress tracking and torrent management
  - Responsive video player with advanced controls
  - Modern, dark-themed UI
- **Target Audience:** Tech-savvy users familiar with torrent technology
- **Primary Goal:** Provide seamless video streaming experience while downloading

## Development Guidelines

### Planning Phase

1. **Component Architecture:** Follow the provided project structure with clear separation of concerns
2. **State Management:** Use React Context for global state (torrents, theme) and custom hooks for complex logic
3. **Real-time Updates:** Implement WebTorrent event listeners for live progress updates
4. **Error Handling:** Robust error boundaries and user-friendly error messages
5. **Performance:** Optimize for large video files and concurrent downloads

### Code Implementation Standards

#### Project Structure
```
src/
├── components/
│   ├── VideoPlayer/          # Video.js integration
│   ├── TorrentSearch/         # Search and results
│   ├── TorrentManager/        # Active torrents management
│   ├── Layout/               # App layout components
│   └── UI/                   # Reusable UI components
├── services/
│   ├── torrentService.ts     # WebTorrent wrapper
│   ├── searchService.ts      # Search API integration
│   └── apiService.ts         # HTTP client
├── types/
│   ├── torrent.ts           # Torrent-related types
│   ├── video.ts             # Video player types
│   └── api.ts               # API response types
├── utils/
│   ├── magnetParser.ts      # Magnet link parsing
│   ├── fileUtils.ts         # File type detection
│   └── constants.ts         # App constants
├── hooks/
│   ├── useTorrent.ts        # Torrent management logic
│   ├── useVideoPlayer.ts    # Video player controls
│   └── useSearch.ts         # Search functionality
├── contexts/
│   └── TorrentContext.tsx   # Global torrent state
└── pages/
    ├── Home.tsx             # Main dashboard
    ├── Player.tsx           # Video player page
    └── Search.tsx           # Search page
```

#### Component Guidelines

1. **Functional Components + Hooks:** Use modern React patterns exclusively
2. **TypeScript Interfaces:** Strict typing for all props and state
3. **Material-UI Integration:**
   - Use MUI components as building blocks
   - Implement custom theme with dark mode support
   - Utilize MUI's responsive breakpoint system
   - Follow Material Design principles
4. **Performance Optimization:**
   - `React.memo` for expensive re-renders
   - `useMemo` and `useCallback` for heavy computations
   - Lazy loading for large components
   - Efficient WebTorrent cleanup

#### State Management

1. **Global State:** TorrentContext for active torrents, download progress, and app settings
2. **Local State:** Component-specific state with useState/useReducer
3. **Custom Hooks:** Encapsulate complex logic (torrent management, video controls)
4. **Immutable Updates:** Always use immutable patterns for state updates

#### Styling with Material-UI

1. **Theme Configuration:**
   ```typescript
   const theme = createTheme({
     palette: {
       mode: 'dark',
       primary: { main: '#bb86fc' },
       secondary: { main: '#03dac6' },
       background: { default: '#121212', paper: '#1e1e1e' }
     }
   });
   ```

2. **Styling Approaches:**
   - **Preferred:** MUI's `sx` prop for component-specific styles
   - **Alternative:** `styled()` API for reusable styled components
   - **Avoid:** Inline styles except for dynamic values

3. **Responsive Design:**
   - Use MUI's breakpoint system (`theme.breakpoints`)
   - Mobile-first approach
   - Flexible grid layouts with `Grid` component

#### WebTorrent Integration

1. **Service Layer:** Centralized torrent management in `torrentService.ts`
2. **Event Handling:** Proper cleanup of WebTorrent event listeners
3. **Progress Tracking:** Real-time updates for download progress
4. **Error Recovery:** Graceful handling of connection issues
5. **Memory Management:** Proper disposal of torrent instances

#### Video Player Integration

1. **Video.js Setup:** Centralized player configuration
2. **Streaming Support:** Handle progressive download streaming
3. **Controls:** Custom controls for torrent-specific features
4. **Formats:** Support multiple video formats and codecs
5. **Responsive:** Adaptive player sizing

## Configuration Files

### tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": "./",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

### vite.config.ts
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { resolve } from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  define: {
    global: 'globalThis'
  },
  server: {
    port: 3000,
    host: true
  },
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor': ['react', 'react-dom'],
          'mui': ['@mui/material', '@emotion/react', '@emotion/styled'],
          'video': ['video.js'],
          'webtorrent': ['webtorrent']
        }
      }
    }
  }
});
```

### package.json (Core Dependencies)
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.23.1",
    "typescript": "^5.0.2",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@mui/material": "^5.15.15",
    "@emotion/react": "^11.11.4",
    "@emotion/styled": "^11.11.5",
    "@mui/icons-material": "^5.15.15",
    "webtorrent": "^2.4.0",
    "@types/webtorrent": "^0.109.7",
    "video.js": "^8.10.0",
    "@types/video.js": "^7.3.58",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0",
    "eslint": "^8.57.0",
    "eslint-plugin-react": "^7.34.1",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.6",
    "@typescript-eslint/eslint-plugin": "^7.2.0",
    "@typescript-eslint/parser": "^7.2.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview"
  }
}
```

## Development Workflow

### Task Execution Process

1. **Requirement Analysis:** Understand the specific torrent/video streaming feature
2. **Component Planning:** Identify affected components and services
3. **Implementation:** Follow the established patterns and architecture
4. **Testing:** Verify functionality with real torrent files (legal content only)
5. **Cleanup:** Ensure proper resource management and error handling

### Code Quality Standards

1. **TypeScript:** Strict typing for all components and services
2. **Error Handling:** Comprehensive error boundaries and user feedback
3. **Performance:** Optimize for large file handling and concurrent operations
4. **Security:** Validate torrent sources and implement content warnings
5. **Documentation:** Clear JSDoc comments for complex torrent logic

### Testing Strategy

1. **Unit Tests:** Service layer and utility functions
2. **Integration Tests:** WebTorrent integration and video player
3. **User Flow Tests:** Complete streaming workflow
4. **Performance Tests:** Memory usage and download efficiency

## Response Format

When working on torrent streaming features, structure responses as:

1. **Feature Overview** (what torrent/video functionality is being implemented)
2. **Technical Approach** (WebTorrent integration, video streaming strategy)
3. **Component Architecture** (how components interact with torrent services)
4. **Implementation Details** (specific code patterns and MUI integration)
5. **Error Handling** (torrent-specific error scenarios)
6. **Performance Considerations** (memory management, streaming optimization)
7. **Legal Compliance** (content warnings, usage guidelines)

## Important Notes

- **Legal Content Only:** Always emphasize legal torrent usage and include appropriate disclaimers
- **Performance Focus:** Prioritize efficient resource usage for large video files
- **User Experience:** Provide clear feedback on download progress and streaming status
- **Cross-Platform:** Ensure compatibility across different browsers and devices
- **Security:** Implement proper validation for torrent sources and magnet links

## Running the Application

```bash
# Development server
npm run dev

# Build for production
npm run build

# Preview production build
npm run preview
```

Default development server runs on `http://localhost:3000`